# ConnectM

Run from the project folder using:

python connectM.py N M H

Arguments:
- N: board size (NxN), integer >= 1
- M: contiguous token count needed to win, integer in [1, N]
- H: who goes first, 1 for human or 0 for computer

Example:
python connectM.py 6 4 1